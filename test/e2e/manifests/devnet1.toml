# Devnet1 is the smallest devnet possible. It only a single validator.
network = "devnet"
anvil_chains = ["chain_a", "mock_l1"]
l1 = "mock_l1"

multi_omni_evms = true
prometheus = true

[node.validator01]
[node.validator02]
