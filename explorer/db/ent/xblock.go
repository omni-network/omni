// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/omni-network/omni/explorer/db/ent/xblock"
)

// XBlock is the model entity for the XBlock schema.
type XBlock struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UUID holds the value of the "uuid" field.
	UUID         uuid.UUID `json:"uuid,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*XBlock) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case xblock.FieldID:
			values[i] = new(sql.NullInt64)
		case xblock.FieldUUID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the XBlock fields.
func (x *XBlock) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case xblock.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			x.ID = int(value.Int64)
		case xblock.FieldUUID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field uuid", values[i])
			} else if value != nil {
				x.UUID = *value
			}
		default:
			x.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the XBlock.
// This includes values selected through modifiers, order, etc.
func (x *XBlock) Value(name string) (ent.Value, error) {
	return x.selectValues.Get(name)
}

// Update returns a builder for updating this XBlock.
// Note that you need to call XBlock.Unwrap() before calling this method if this XBlock
// was returned from a transaction, and the transaction was committed or rolled back.
func (x *XBlock) Update() *XBlockUpdateOne {
	return NewXBlockClient(x.config).UpdateOne(x)
}

// Unwrap unwraps the XBlock entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (x *XBlock) Unwrap() *XBlock {
	_tx, ok := x.config.driver.(*txDriver)
	if !ok {
		panic("ent: XBlock is not a transactional entity")
	}
	x.config.driver = _tx.drv
	return x
}

// String implements the fmt.Stringer.
func (x *XBlock) String() string {
	var builder strings.Builder
	builder.WriteString("XBlock(")
	builder.WriteString(fmt.Sprintf("id=%v, ", x.ID))
	builder.WriteString("uuid=")
	builder.WriteString(fmt.Sprintf("%v", x.UUID))
	builder.WriteByte(')')
	return builder.String()
}

// XBlocks is a parsable slice of XBlock.
type XBlocks []*XBlock
