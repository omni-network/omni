---
layout: landing
---

import { HomePage } from "vocs/components";

<HomePage.Root>
  <img src="/img/Omni_Lockup_Light.png" width="400"/>

  <HomePage.InstallPackage name="@omni-network/react @omni-network/core" type="install" />

{" "}

{" "}

<HomePage.Tagline>
  Onboard your users from any chain in 1 click
</HomePage.Tagline>

<HomePage.Description>
  Omni is creating a world where the entire onchain economy is just 1 click away. Use the Omni SDK to bridge, swap, deposit, and bundle arbitrary actions across any chain. Powered by intents.
</HomePage.Description>

<HomePage.Buttons>
  <HomePage.Button href="/introduction/welcome" variant="accent">
    Get started
  </HomePage.Button>
</HomePage.Buttons>

<div style={{ display: 'flex', gap: '20px', marginTop: '40px', justifyContent: 'center' }}>
  <div style={{
    backgroundColor: '#f8f9fa',
    border: '1px solid #e9ecef',
    borderRadius: '8px',
    padding: '24px',
    textAlign: 'center',
    flex: '1',
    maxWidth: '280px',
    transition: 'transform 0.2s ease, box-shadow 0.2s ease',
    cursor: 'pointer'
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.transform = 'translateY(-2px)';
    e.currentTarget.style.boxShadow = '0 4px 12px rgba(0,0,0,0.1)';
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.transform = 'translateY(0)';
    e.currentTarget.style.boxShadow = 'none';
  }}
  onClick={() => window.location.href = '/sdk/getting-started/react'}>
    <h3 style={{ margin: '0 0 12px 0', color: '#495057', pointerEvents: 'none' }}>Get started with React</h3>
    <p style={{ margin: 0, color: '#6c757d', fontSize: '14px', pointerEvents: 'none' }}>Build React applications powered by Omni</p>
  </div>

  <div style={{
    backgroundColor: '#f8f9fa',
    border: '1px solid #e9ecef',
    borderRadius: '8px',
    padding: '24px',
    textAlign: 'center',
    flex: '1',
    maxWidth: '280px',
    transition: 'transform 0.2s ease, box-shadow 0.2s ease',
    cursor: 'pointer'
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.transform = 'translateY(-2px)';
    e.currentTarget.style.boxShadow = '0 4px 12px rgba(0,0,0,0.1)';
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.transform = 'translateY(0)';
    e.currentTarget.style.boxShadow = 'none';
  }}
  onClick={() => window.location.href = '/sdk/getting-started/core'}>
    <h3 style={{ margin: '0 0 12px 0', color: '#495057', pointerEvents: 'none' }}>Get started with Core</h3>
    <p style={{ margin: 0, color: '#6c757d', fontSize: '14px', pointerEvents: 'none' }}>Build TS or scripts with any framework</p>
  </div>

  <div style={{
    backgroundColor: '#f8f9fa',
    border: '1px solid #e9ecef',
    borderRadius: '8px',
    padding: '24px',
    textAlign: 'center',
    flex: '1',
    maxWidth: '280px',
    transition: 'transform 0.2s ease, box-shadow 0.2s ease',
    cursor: 'pointer'
  }}
  onMouseEnter={(e) => {
    e.currentTarget.style.transform = 'translateY(-2px)';
    e.currentTarget.style.boxShadow = '0 4px 12px rgba(0,0,0,0.1)';
  }}
  onMouseLeave={(e) => {
    e.currentTarget.style.transform = 'translateY(0)';
    e.currentTarget.style.boxShadow = 'none';
  }}
  onClick={() => window.open('https://github.com/omni-network/omni', '_blank')}>
    <h3 style={{ margin: '0 0 12px 0', color: '#495057', pointerEvents: 'none' }}>Explore our Monorepo</h3>
    <p style={{ margin: 0, color: '#6c757d', fontSize: '14px', pointerEvents: 'none' }}>Browse the full Omni codebase on GitHub</p>
  </div>
</div>

</HomePage.Root>
