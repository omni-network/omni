{
  "name": "omni-sdk",
  "private": true,
  "description": "Omni SDK workspace",
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build": "cd packages/react && pnpm build",
    "check": "biome check --write",
    "test:unit": "cd packages/react && pnpm test",
    "test:integration": "cd integration-tests && pnpm test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
